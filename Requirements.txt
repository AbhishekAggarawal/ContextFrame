# ── ContextFrame Backend ── requirements.txt ──────────────────────────────────
# Python 3.12 required (pydub needs audioop which was removed in 3.13+)
#
# ALL deps run on Render's 512 MB free tier:
#   • STT → Sarvam AI cloud API (free, user has key)
#   • RAG → BM25 keyword retriever (pure Python, ~0 MB RAM)
#   • LLM → Mistral cloud API
#
# No torch, no whisper, no chromadb, no sentence-transformers, no huggingface.

# ── Web Server (FastAPI) ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9            # Required by FastAPI for Form data / file uploads

# ── Audio / Video acquisition ─────────────────────────────────────────────────
youtube-transcript-api>=1.2.0     # Fetch YouTube auto-generated captions (no bot blocking)
pydub>=0.25.1                     # Audio file manipulation / format conversion

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-mistralai>=0.1.0        # LangChain wrapper for Mistral API
langchain-text-splitters>=0.2.0   # Text chunking utilities
mistralai>=0.4.0                  # Official Mistral Python client

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0              # Load API keys from .env
rank-bm25>=0.2.2                  # BM25 keyword retrieval (pure Python, ~0 MB RAM)
numpy>=1.26.0
tqdm>=4.66.0                      # Progress bars
requests>=2.32.0